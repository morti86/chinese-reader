# AGENTS.md file

- This is a Rust desktop application
- Find the CI plan in the .github/workflows folder

## Build commands
- `cargo build` for building
- `cargo test` for tests
- `cargo clippy` for some code quality checks
