# AGENTS.md file

- This is a Rust desktop application
- Find the CI plan in the .github/workflows folder

## Build commands
- `cargo build` for building
- `cargo test` for tests
- `cargo clippy` for some code quality checks

## What to do
- audit the application for possible issues
- if some parts can be optimized, find ways to do that
- do not make changes to the code, only write the report to `audit.md` file

## Audit report
- point to the problematic part
- explain the issue
- suggest improvements and fixes
